{
  "name": "user-service",
  "version": "1.0.0",
  "description": "Provider service (UserService) for PactJS contract testing example",
  "scripts": {
    "test": "jest tests --runInBand",
    "broker:up": "docker compose -p pact-broker up -d",
    "broker:down": "docker compose -p pact-broker down",
    "pact:can-i-deploy": "dotenv -e .env -- pact-broker can-i-deploy --pacticipant UserService --version=$(git rev-parse --short HEAD) --to-environment production",
    "pact:record-deployment": "dotenv -e .env -- pact-broker record-deployment --pacticipant UserService --version=$(git rev-parse --short HEAD) --environment=production"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@pact-foundation/pact": "^13.1.4",
    "@pact-foundation/pact-cli": "^18.1.1",
    "dotenv": "^17.4.2",
    "dotenv-cli": "^11.0.0",
    "jest": "^29.7.0"
  }
}
